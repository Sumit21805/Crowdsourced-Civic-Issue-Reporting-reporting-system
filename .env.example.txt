# Optional: JWT secret (defaults to a dev value)
# JWT_SECRET=your-secret

# Optional: SMTP for sending report notifications to department
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=user
# SMTP_PASS=pass
# SMTP_FROM=noreply@example.com

# Email address that receives new report notifications
# DEPARTMENT_EMAIL=department@example.com

# Optional: when in production, protect seed with a secret header
# SEED_SECRET=your-seed-secret