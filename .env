# SMTP (Gmail example – use App Password, not account password)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465 #apparently 587 better but 465 is working
SMTP_USER=aditya.verma262007@gmail.com
SMTP_PASSWORD=apna password generate karo

# Who receives the issue notifications (defaults to SMTP_USER if not set)
NOTIFY_EMAIL=recklessfsm@gmail.com

# Optional: for testing send_issue_email.py directly
# TEST_REPORTER=reporter@example.com
